include("std");

ya = type.number;

xe = enum {
    a,
    b,
    c
};

ye = xe;

x = xe.b;

y = ye.a;

data = {
    v = y;
};
register = function;
export(&register);
native(&register);
register(data);